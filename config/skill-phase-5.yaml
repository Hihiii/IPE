schema_version: "1.0.0"
skill_phase:
  phase: 5
  name: "Phase 5: Z-Image Final Output"
  internal: false
  instructions: |
    Return exactly three fields: z_image_positive_prompt,
    z_image_negative_prompt, and suggest_resolution. Prompt text must be
    English. Do not create files unless the user explicitly requests an export
    to an absolute path. Do not return Flux or any other model output.

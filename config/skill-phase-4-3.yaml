schema_version: "1.0.0"
skill_phase:
  phase: 4.3
  name: "Phase 4.3: Z-Image Delivery Package"
  internal: true
  instructions: |
    Normalize the Phase 4.2 Z-Image result without adding or removing visual
    decisions. Keep positive_prompt, negative_prompt, locked constraints, and
    suggest_resolution. Verify that no raw internal package fields leak into
    delivery and that the selected target remains `z_image`.
